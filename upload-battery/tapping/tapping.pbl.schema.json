{
  "parameters": [
    {
      "name": "trialtime",
      "type": "integer",
      "default": 10,
      "description": "Duration of each trial in seconds"
    },
    {
      "name": "maxtrials",
      "type": "integer",
      "default": 8,
      "description": "Maximum number of trials per hand"
    },
    {
      "name": "usekeypad",
      "type": "integer",
      "default": 0,
      "description": "Use keyboard number pad (0=use A key, 1=use keypad 0)"
    },
    {
      "name": "demo_override",
      "type": "integer",
      "default": 0,
      "description": "Run short demo version (1=demo with reduced trials, 0=full test)",
      "options": {
        "trialtime": 5,
        "maxtrials": 3
      }
    }
  ]
}
