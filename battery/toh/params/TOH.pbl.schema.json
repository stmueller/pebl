{
  "test": "TOH",
  "version": "1.0",
  "description": "TOH Test",
  "parameters": [
    {
      "name": "showhand",
      "type": "boolean",
      "default": 1,
      "description": "Whether hand should be shown",
      "options": [
        0,
        1
      ]
    },
    {
      "name": "problemset",
      "type": "string",
      "default": "random3",
      "description": "Which problem set to use. currently random3 or bishop are possible."
    },
    {
      "name": "numtrials",
      "type": "integer",
      "default": 10,
      "description": "number of trials for random sets"
    },
    {
      "name": "usetimer",
      "type": "boolean",
      "default": 0,
      "description": "Whether timer should be used",
      "options": [
        0,
        1
      ]
    },
    {
      "name": "timelimit",
      "type": "integer",
      "default": 120,
      "description": "Time limit, in seconds, if time limit is used."
    },
    {
      "name": "useturnlimit",
      "type": "boolean",
      "default": 0,
      "description": "whether you require the best path",
      "options": [
        0,
        1
      ]
    }
  ]
}