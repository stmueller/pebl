define Start(p)
{ 
  gVideoWidth<-800
  gVideoHeight<-600
  gWin <- MakeWindow("grey90")
  easy <- EasyLabel("pRESS any key to select file",400,100,gWin,40)
  Draw()
  
  WaitForANYkEYpRESS()
  file <- (FilePicker(".",".pbl"))
  easy.text <- file
  Print(file)
  Draw()

  WaitForAnyKeyPress()

}

