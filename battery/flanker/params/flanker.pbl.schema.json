{
  "test": "flanker",
  "version": "1.0",
  "description": "FLANKER Test",
  "parameters": [
    {
      "name": "numreps",
      "type": "integer",
      "default": 20,
      "description": "Number of repeats of 2x3 design"
    },
    {
      "name": "practicereps",
      "type": "integer",
      "default": 2,
      "description": "Number of repetitions of desing in practice"
    },
    {
      "name": "includedashtrials",
      "type": "boolean",
      "default": 1,
      "description": "whether -->-- and --<-- trials should be included",
      "options": [
        0,
        1
      ]
    },
    {
      "name": "includeemptytrials",
      "type": "boolean",
      "default": 1,
      "description": "whether [  >  ] and [  <  ] trials should be used",
      "options": [
        0,
        1
      ]
    },
    {
      "name": "arrowsize",
      "type": "integer",
      "default": 40,
      "description": "Pixel length of arrow (not head, entire arrow)"
    },
    {
      "name": "gap",
      "type": "integer",
      "default": 2,
      "description": "pixel gap between arrows"
    },
    {
      "name": "fixationtime",
      "type": "integer",
      "default": 500,
      "description": "Time prior to trial that fixation appears, in ms"
    },
    {
      "name": "timeout",
      "type": "integer",
      "default": 800,
      "description": "Time allotted to make response, in ms"
    },
    {
      "name": "iti",
      "type": "integer",
      "default": 1000,
      "description": "Inter-trial interval; time between response and new trial strarting, in ms"
    },
    {
      "name": "responsemode",
      "type": "string",
      "default": "auto",
      "options": ["auto", "keyboardShift", "keyboardSafe", "mousetarget", "mousebutton"],
      "label": "Response Mode",
      "description": "Response method: auto (platform-aware), keyboardShift (L/R shift), keyboardSafe (Z/slash for web), mousetarget (click targets), mousebutton (L/R mouse buttons)"
    }
  ]
}