{
  "test": "donkey",
  "version": "1.0",
  "description": "DONKEY Test",
  "parameters": [
    {
      "name": "inputdevice",
      "type": "string",
      "default": "mouse",
      "description": "Method to use for input. mouse or keyboard are valid. if keyboard, uses cvbn keys to make responses."
    },
    {
      "name": "numtrials",
      "type": "integer",
      "default": 150,
      "description": "Number of trials to use.  Note that each 'deck' has only 40 rewards that get rotated and not shuffled."
    }
  ]
}