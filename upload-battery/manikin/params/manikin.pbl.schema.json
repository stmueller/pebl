{
  "test": "manikin",
  "version": "1.0",
  "description": "PEBL Manikin Test - 3D Spatial Reorientation",
  "parameters": [
    {
      "name": "numBlocks",
      "type": "integer",
      "default": 3,
      "demo_override": 1,
      "description": "Number of test blocks to complete (each block contains 16 trials)."
    },
      {
	  "name": "zoom",
	  "type": "float",
	  "default": 0.6,
	  "description": "Size of displayed manikin (scale factor)"
      },
      {
	  "name": "iti",
	  "type": "integer",
	  "default": 750,
	  "description": "Wait time between trials in ms"
      },
      {
	  "name": "responsemode",
	  "type": "string",
	  "default": "auto",
	  "options": ["auto", "keyboardShift", "keyboardSafe", "arrowLR", "mousetarget", "mousebutton"],
	  "description": "Response method: auto (platform-aware), keyboardShift (L/R shift), keyboardSafe (Z/slash for web), arrowLR (left/right arrows), mousetarget (click targets), mousebutton (L/R mouse buttons)"
      }
  ]
}
