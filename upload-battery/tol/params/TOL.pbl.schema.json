{
  "test": "TOL",
  "version": "1.0",
  "description": "TOL Test",
  "parameters": [
    {
      "name": "showhand",
      "type": "boolean",
      "default": 1,
      "description": "Whether to show the hand graphic. Set from 1 to 0 to hide hand.",
      "options": [
        0,
        1
      ]
    },
    {
      "name": "useReverse",
      "type": "boolean",
      "default": 0,
      "description": "Whether to reverse the piles (i.e., low to hi versus hi to low). Some tests do this automatically.",
      "options": [
        0,
        1
      ]
    },
    {
      "name": "testtype",
      "type": "integer",
      "default": 3,
      "demo_override":1,
      "description": "Test configuration: 1=Progressive 3-5 disks, 2=Random 3-5 disks, 3=Shallice 12 problems, 4=Shallice 30 random, 5=Phillips A, 6=Phillips B, 7=Phillips C, 8=Fimbel Old (15 trials), 9=Fimbel Young (35 trials), 0=TOL-R (30 trials), 10=TOL-DX (15 trials)"
    },
    {
      "name": "timelimit",
      "type": "integer",
      "default": 120,
      "description": "Time limit, in seconds, for the test (when gUseTimer is non-zero, which is set in each test)"
    },
    {
      "name": "allowreset",
      "type": "boolean",
      "default": 0,
      "description": "Whether to allow the problem to be 'reset' by subject.",
      "options": [
        0,
        1
      ]
    },
    {
      "name": "allowskip",
      "type": "boolean",
      "default": 0,
      "description": "Whether to allow the problem to be 'skipped' by subject.",
      "options": [
        0,
        1
      ]
    }
  ]
}
