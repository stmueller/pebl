{
  "test": "switcher",
  "version": "1.0",
  "description": "SWITCHER Test",
  "parameters": [
    {
      "name": "trials",
      "type": "integer",
      "default": 12,
      "description": "Number of trials to test in each condition."
    },
    {
      "name": "fieldwidth",
      "type": "integer",
      "default": 400,
      "description": "Width, in pixels, of the field the targets appear in."
    },
    {
      "name": "fieldheight",
      "type": "integer",
      "default": 400,
      "description": "Height, in pixels, of the field the targets appear in."
    }
  ]
}