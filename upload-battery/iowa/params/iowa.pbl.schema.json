{
  "test": "iowa",
  "version": "1.0",
  "description": "IOWA Test",
  "parameters": [
    {
      "name": "numtrials",
      "type": "integer",
      "default": 100,
      "description": "Number of trials to use. Note that there are 40 cards in each deck=120, and so cards get rotated without being shuffled if used up"
    },
    {
      "name": "UseMouse",
      "type": "boolean",
      "default": 1,
      "description": "Should we use the mouse  (use=1;don't use=0 for the keyboard)",
      "options": [
        0,
        1
      ]
    },
    {
      "name": "shuffledecks",
      "type": "boolean",
      "default": 0,
      "description": "whether decks should be shuffled.  Shuffling happens in epochs of size 10, to maintain the reward structure within sets of 10.   The default is off",
      "options": [
        0,
        1
      ]
    },
    {
      "name": "startvalue",
      "type": "integer",
      "default": 2000,
      "description": "Starting money"
    },
    {
      "name": "rewarddelay",
      "type": "integer",
      "default": 2000,
      "description": "How long should the reward feedback take?"
    },
    {
      "name": "penaltydelay",
      "type": "integer",
      "default": 2000,
      "description": "How long should the reward feedback take?"
    },
    {
      "name": "playsounds",
      "type": "boolean",
      "default": 1,
      "description": "Should sounds be played?",
      "options": [
        0,
        1
      ]
    }
  ]
}
