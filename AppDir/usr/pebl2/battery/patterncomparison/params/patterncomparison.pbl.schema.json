{
  "test": "patterncomparison",
  "version": "1.0",
  "description": "PATTERNCOMPARISON Test",
  "parameters": [
    {
      "name": "similarfoil",
      "type": "boolean",
      "default": 1,
      "description": "should foil be similar to target",
      "options": [
        0,
        1
      ]
    },
    {
      "name": "foildiff",
      "type": "integer",
      "default": 3,
      "description": "Number of changes between target and foil"
    },
    {
      "name": "size",
      "type": "integer",
      "default": 4,
      "description": "size of the target/foil matrices"
    },
    {
      "name": "givefeedback",
      "type": "boolean",
      "default": 1,
      "description": "Whether to give feedback",
      "options": [
        0,
        1
      ]
    },
    {
      "name": "condsize",
      "type": "integer",
      "default": 10,
      "description": "Number of trials per condition (x12)"
    },
    {
      "name": "deadline",
      "type": "integer",
      "default": 1500,
      "description": "Time in ms to give a response"
    }
  ]
}