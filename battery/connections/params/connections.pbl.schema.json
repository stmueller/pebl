{
  "test": "connections",
  "version": "1.0",
  "description": "CONNECTIONS Test",
  "parameters": [
    {
      "name": "inputdevice",
      "type": "string",
      "default": "mouse",
      "description": "Device to use. Currently, makes no difference.  A keyboard entry method could be implemented."
    },
    {
      "name": "width",
      "type": "integer",
      "default": 7,
      "description": "Width of test grid"
    },
    {
      "name": "height",
      "type": "integer",
      "default": 7,
      "description": "Height of test grid"
    },
    {
      "name": "timelimit",
      "type": "integer",
      "default": 20,
      "description": "Time limit in seconds"
    }
  ]
}