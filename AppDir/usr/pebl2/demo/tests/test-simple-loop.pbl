## Test just a simple loop without recursion

define Start(p)
{
    Print("Testing simple loop...")

    counter <- 0
    loop(i, Sequence(1, 10, 1))
    {
        counter <- counter + 1
        Print("Loop iteration: " + i)
    }

    Print("Loop completed, counter = " + counter)
}
