{
  "test": "tlx",
  "version": "1.0",
  "description": "TLX Test",
  "parameters": [
    {
      "name": "doweightings",
      "type": "boolean",
      "default": 0,
      "description": "Should you do the weightings as well as the ratings?",
      "options": [
        0,
        1
      ]
    },
    {
      "name": "taskname",
      "type": "string",
      "default": "Target Task",
      "description": "Name of the task at the top of the screen."
    },
    {
      "name": "scaleheight",
      "type": "integer",
      "default": 300,
      "description": "Height in pixels of the scale."
    }
  ]
}
