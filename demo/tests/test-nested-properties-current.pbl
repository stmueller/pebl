## Test what happens with current nested property syntax
define Start(p)
{
    Print("Testing current nested property behavior...")

    ## Create a simple custom object with nested structure
    gObj <- MakeCustomObject()
    gObj.header <- MakeCustomObject()
    gObj.header.text <- "Initial"

    ## Try to read nested property - what happens?
    Print("Attempting to read gObj.header.text...")
    x <- gObj.header.text
    Print("Read value: " + x)

    ## Try to write nested property - what happens?
    Print("Attempting to write gObj.header.text...")
    gObj.header.text <- "Modified"

    Print("Test completed")
}
