define Start(p)
{
  gVideoWidth <- 300
  gVideoHeight <-300
  gWin <- MakeWindow("black")
  Draw()
  gLastUpdate<- GetTime()
  dummy <- EasyLabel("one",150,150,gWin,14)
  out <- WaitForAnyKeyPress()
  Print(out)
  Print(GetObjectTime(out))
  Print(GetTime())


}
