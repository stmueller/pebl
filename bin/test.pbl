define Start(p)
{
gVideoWidth <- 800
gVideoHeight <- 600
win <- MakeWindow("green")
Draw()
Wait(1000)
## do a busy-wait to ensure the waiting works.
loop(i,10000)
{
  Print_(".")
}
Print("-----------------------------------")
Print("-----------------------------------")
Print("-----------------------------------")
Print("-----------------------------------")
Print("-----------------------------------")

}