{
  "test": "randomgeneration",
  "version": "1.0",
  "description": "RANDOMGENERATION Test",
  "parameters": [
    {
      "name": "numtrials",
      "type": "integer",
      "default": 162,
      "description": "Number of total trials to use."
    },
    {
      "name": "practicetrials",
      "type": "integer",
      "default": 15,
      "description": "Number of practice trials"
    },
    {
      "name": "isi",
      "type": "integer",
      "default": 800,
      "description": "ISI in ms for the beeps"
    },
    {
      "name": "usebeep",
      "type": "boolean",
      "default": 1,
      "description": "Whether to use a beep to guide responses",
      "options": [
        0,
        1
      ]
    },
    {
      "name": "useflash",
      "type": "boolean",
      "default": 1,
      "description": "Whether to use a flash to guide responses",
      "options": [
        0,
        1
      ]
    }
  ]
}