{
  "parameters": [
    {
      "name": "testtype",
      "type": "integer",
      "default": 3,
      "description": "Test type configuration"
    },
    {
      "name": "usecontinuous",
      "type": "boolean",
      "default": true,
      "description": "Use continuous stimulus presentation timing"
    },
    {
      "name": "givefeedback",
      "type": "boolean",
      "default": true,
      "description": "Provide feedback during the test"
    },
    {
      "name": "fixationtime",
      "type": "integer",
      "default": 400,
      "description": "Duration fixation cross remains visible (milliseconds)"
    },
    {
      "name": "numBlocks",
      "type": "integer",
      "default": 12,
      "description": "Number of repetitions of the interval set"
    },
    {
      "name": "numTrials",
      "type": "integer",
      "default": 120,
      "description": "Total number of trials (120 ~= 10 minutes)"
    },
    {
      "name": "totalTime",
      "type": "integer",
      "default": 600,
      "description": "Total time of the test in milliseconds"
    },
    {
      "name": "TooFast",
      "type": "integer",
      "default": 150,
      "description": "RT threshold for marking trial as too fast (milliseconds)"
    },
    {
      "name": "Lapse",
      "type": "integer",
      "default": 500,
      "description": "RT threshold for marking trial as lapse (milliseconds)"
    },
    {
      "name": "Sleepattack",
      "type": "integer",
      "default": 30000,
      "description": "RT threshold for marking trial as sleep attack (milliseconds)"
    },
    {
      "name": "responsemode",
      "type": "string",
      "default": "spacebar",
      "options": ["spacebar", "leftclick", "touchscreen", "clicktarget"],
      "description": "Response method: spacebar (space key), leftclick (left mouse button), touchscreen (tap anywhere), clicktarget (click target box)"
    }
  ]
}
