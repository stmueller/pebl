{
  "test": "typing",
  "version": "1.0",
  "description": "TYPING Test",
  "parameters": [
    {
      "name": "duration",
      "type": "string",
      "default": "short",
      "description": "Short versus long (1-min v 3-min"
    },
    {
      "name": "wordtype",
      "type": "string",
      "default": "choose",
      "description": "words, random, or choose (to choose in-test)"
    },
    {
      "name": "set",
      "type": "boolean",
      "default": 1,
      "description": "Which stimulus set to use (1-4 possible)",
      "options": [
        0,
        1
      ]
    }
  ]
}