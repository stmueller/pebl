{
  "test": "number-stroop",
  "version": "1.0",
  "description": "NUMBER-STROOP Test",
  "parameters": [
    {
      "name": "responsetype",
      "type": "string",
      "default": "choose",
      "description": "Input method.  'choose' lets you choose in-task.  'keyboard' uses keyboard responses, 'keypad' uses keypad, 'audio' uses voicekey (which can be a bit buggy)."
    },
    {
      "name": "scorerealtime",
      "type": "boolean",
      "default": 0,
      "description": "For audio, set this to 1 to score real-time with keyboard. otherwise, you need to go back through and code responses afterward.",
      "options": [
        0,
        1
      ]
    },
    {
      "name": "usepreset",
      "type": "boolean",
      "default": 1,
      "description": "If set to 1, it will use the same sequence of stimuli every time.  If set to 0, it will  regenerate randomly on each run.",
      "options": [
        0,
        1
      ]
    },
    {
      "name": "nonpresettrials",
      "type": "integer",
      "default": 28,
      "description": "If usepreset is 0, the blocks (after the practice block) will be auto-generated with this many trials per condition."
    },
    {
      "name": "fixationTime",
      "type": "integer",
      "default": 1000,
      "description": "time for the fixation cross"
    },
    {
      "name": "responseTimeout",
      "type": "integer",
      "default": 2000,
      "description": "Time limit given for responding"
    },
    {
      "name": "voicekeythreshold",
      "type": "float",
      "default": 0.35,
      "description": "The voice key will trip when this proportion of the last gVoiceKeyDuration 1-ms bins are above the threshold. Based on input power and microphone noise, you may need to adjust these parameters."
    },
    {
      "name": "voiceKeyDuration",
      "type": "integer",
      "default": 200,
      "description": "Number of bins back to test for being above threshold for the voice key"
    },
    {
      "name": "saveAudio",
      "type": "boolean",
      "default": 1,
      "description": "Should the audio buffer be saved to a file?",
      "options": [
        0,
        1
      ]
    },
    {
      "name": "compressAudio",
      "type": "boolean",
      "default": 0,
      "description": "Should .wav files be compressed? 0=no, 1 = yes",
      "options": [
        0,
        1
      ]
    },
    {
      "name": "compressor",
      "type": "string",
      "default": "oggenc",
      "description": "Command to compress audio"
    }
  ]
}