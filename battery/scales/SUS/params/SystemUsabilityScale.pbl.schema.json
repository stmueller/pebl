{
  "parameters": [
    {
      "name": "do_intro",
      "type": "boolean",
      "default": true,
      "description": "Show introductory instruction screen"
    },
    {
      "name": "do_outro",
      "type": "boolean",
      "default": true,
      "description": "Show completion screen at end"
    },

      {
	  "name": "system_name",
	  "type": "string",
	  "default": "the system",
	  "description": "Use a more descriptive term for the system your are rating"
      }
  ]
}
