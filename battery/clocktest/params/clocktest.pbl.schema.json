{
  "test": "clocktest",
  "version": "1.0",
  "description": "CLOCKTEST Test",
  "parameters": [
    {
      "name": "pskip",
      "type": "float",
      "default": 0.4,
      "description": "Probability that any trial will be a 'skip' trial"
    },
    {
      "name": "radius",
      "type": "integer",
      "default": 240,
      "description": "Radius of clock circle, in pixels."
    },
    {
      "name": "size",
      "type": "integer",
      "default": 9,
      "description": "Radius of target dots, in pixels"
    },
    {
	"name": "trials",
	"type": "integer",
	"default": 300,
	"demo_override", 30,
	"description": "Number of (1-second) trials to test."
    },
    {
      "name": "responsemode",
      "type": "string",
      "default": "spacebar",
      "options": ["spacebar", "leftclick", "touchscreen"],
      "description": "Response method: spacebar (keyboard), leftclick (click anywhere), touchscreen (tap anywhere)"
    }
  ]
}
