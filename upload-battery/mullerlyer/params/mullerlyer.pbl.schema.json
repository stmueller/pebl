{
  "test": "mullerlyer",
  "version": "1.0",
  "description": "Muller-Lyer Illusion Task",
  "parameters": [
    {
      "name": "prestime",
      "type": "integer",
      "default": 400,
      "description": "how long to do the presentation, in ms"
    },
    {
      "name": "trials",
      "type": "integer",
      "default": 50,
      "description": "Number of trials"
    },
    {
      "name": "spread",
      "type": "integer",
      "default": 125,
      "description": "How many pixels left and right of the centerline will the stimuli be"
    },
    {
      "name": "giveFeedback",
      "type": "boolean",
      "default": 0,
      "description": "whether feedback should be given",
      "options": [
        0,
        1
      ]
    },
    {
      "name": "stepsize",
      "type": "integer",
      "default": 10,
      "description": "How big of a step to make after judgement?"
    },
    {
      "name": "randomstep",
      "type": "integer",
      "default": 16,
      "description": "How much randomness to include in step?"
    },
    {
      "name": "diff",
      "type": "integer",
      "default": -40,
      "description": "start diff value.  It should be somewhat negative."
    },
    {
      "name": "responsemode",
      "type": "string",
      "default": "auto",
      "options": [
        "auto",
        "keyboardShift",
        "keyboardSafe",
        "arrowLR",
        "mousetarget",
        "mousebutton",
        "userselect",
        "touchtarget"
      ],
      "label": "Response Mode",
      "description": "Response method for left/right judgment"
    },
    {
      "name": "responselabels",
      "type": "list",
      "default": [
        "LEFT",
        "RIGHT"
      ],
      "description": "Semantic labels for left and right responses"
    }
  ]
}
