## Absolute minimal test

define Start(p)
{
  gWin <- MakeWindow("black")
  Print("Creating label...")
  label <- EasyLabel("Test", 400, 300, gWin, 28)
  Print("Label created successfully!")
  WaitForAnyKeyPress()
}
