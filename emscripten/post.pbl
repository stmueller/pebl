define Start(p)
{


  host <- "localhost:8080"
  page <- "/upload.php"

  content <- "filename=post.pbl"

   ##dummy:  
  headers <- ["Connection", "close",
              "Content-type","multipart/form-data" ]

  savename <- "test.html"


  
  out <-  PostHTTP(host,page,headers,content)
  Print("-------------------")
  Print(out)
  Print("-------------------")
} 
