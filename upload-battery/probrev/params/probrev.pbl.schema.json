{
  "test": "probrev",
  "version": "1.0",
  "description": "PROBREV Test",
  "parameters": [
    {
      "name": "numblocks",
      "type": "integer",
      "default": 4,
      "description": "Each block is around 150 trials."
    },
    {
      "name": "numstages",
      "type": "integer",
      "default": 10,
      "description": "Number of learning 'stages' per block; after which a reversal happens"
    },
    {
      "name": "stagemin",
      "type": "integer",
      "default": 10,
      "description": "Smallest stage size"
    },
    {
      "name": "stagemax",
      "type": "integer",
      "default": 15,
      "description": "Largest stage size"
    },
    {
      "name": "errorMin",
      "type": "boolean",
      "default": 0,
      "description": "smallest number of invalid per block",
      "options": [
        0,
        1
      ]
    },
    {
      "name": "errorMax",
      "type": "integer",
      "default": 5,
      "description": "largest number of invalid per block"
    }
  ]
}