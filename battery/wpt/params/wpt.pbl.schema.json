{
  "test": "wpt",
  "version": "1.0",
  "description": "WPT Test",
  "parameters": [
    {
      "name": "numtrials",
      "type": "integer",
      "default": 350,
      "description": "Number of trials to test"
    },
    {
      "name": "startpoints",
      "type": "integer",
      "default": 600,
      "description": "number of points to start with"
    },
    {
      "name": "maxpoints",
      "type": "integer",
      "default": 1000,
      "description": "top value of the graph"
    },
    {
      "name": "blocksize",
      "type": "integer",
      "default": 50,
      "description": "Number of trials between break."
    },
    {
      "name": "responsemode",
      "type": "string",
      "default": "auto",
      "options": ["auto", "keyboardShift", "keyboardSafe", "arrowLR", "mousetarget", "touchtarget", "mousebutton"],
      "description": "Response method: auto (platform-aware), keyboardShift (L/R shift), keyboardSafe (Z/slash for web), arrowLR (left/right arrows), mousetarget (click targets), touchtarget (tap targets), mousebutton (L/R mouse buttons)"
    }
  ]
}