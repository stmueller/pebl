{
  "test": "probmonitor",
  "version": "1.0",
  "description": "PROBMONITOR Test",
  "parameters": [
    {
      "name": "delay",
      "type": "integer",
      "default": 50,
      "description": "Update timeframe, in ms"
    },
    {
      "name": "signals",
      "type": "integer",
      "default": 10,
      "description": "Number of updates per trial"
    },
    {
      "name": "mintime",
      "type": "integer",
      "default": 4000,
      "description": "shortest time (in ms) before signal happens"
    },
    {
      "name": "maxtime",
      "type": "integer",
      "default": 5000,
      "description": "longest time (in ms) before signal happens"
    },
    {
      "name": "triallength",
      "type": "integer",
      "default": 300,
      "description": "Number of seconds of the trial"
    },
    {
      "name": "minMonitors",
      "type": "boolean",
      "default": 1,
      "description": "Minumum number of monitors to observe",
      "options": [
        0,
        1
      ]
    },
    {
      "name": "maxMonitors",
      "type": "integer",
      "default": 3,
      "description": "Max number of monitors to observe (at most, 3)"
    }
  ]
}