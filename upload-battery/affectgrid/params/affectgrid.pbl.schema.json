{
  "test": "affectgrid",
  "version": "1.0",
  "description": "PEBL Affect Grid - Self-reported emotion assessment",
  "parameters": [
    {
      "name": "fg",
      "type": "string",
      "default": "grey20",
      "description": "Foreground color for grid squares",
      "options": ["grey20", "black", "gray", "darkgray"]
    },
    {
      "name": "bg",
      "type": "string",
      "default": "white",
      "description": "Background color for grid and window",
      "options": ["white", "lightgray", "gray"]
    },
    {
      "name": "squaresize",
      "type": "integer",
      "default": 60,
      "description": "Size of each grid square in pixels (affects overall grid size)"
    },
    {
      "name": "border",
      "type": "integer",
      "default": 5,
      "description": "Border spacing between grid squares in pixels"
    }
  ]
}
