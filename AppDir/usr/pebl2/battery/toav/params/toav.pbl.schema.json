{
  "test": "toav",
  "version": "1.0",
  "description": "TOAV Test",
  "parameters": [
    {
      "name": "numRare",
      "type": "integer",
      "default": 72,
      "description": "Number of rare stimuli per half"
    },
    {
      "name": "numCommon",
      "type": "integer",
      "default": 248,
      "description": "Number of common stimuli per half"
    },
    {
      "name": "doheader",
      "type": "boolean",
      "default": 1,
      "description": "Whether a header should appear during test reminding participant of response key and mapping.",
      "options": [
        0,
        1
      ]
    }
  ]
}